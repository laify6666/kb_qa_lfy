# kb_qa 调优日志（2026-08，本机扩充与调优记录）

> 目标：把项目从「复现可用」推进到「481 篇真实语料 + 可量化、可复现的调优」。
> 全程原则：**先量化基线，再单变量实验；每个结论必须能指到一行证据；区分「语料覆盖缺口」与「系统错误」，绝不靠改题自欺。**

---

## 0. 起点与前置工作

- 语料：5 篇 → **481 篇**（维基百科 120 / CMRC2018 100 / DRCD 80 / Langchain-Chatchat 25 / THUCNews 镜像 150；来源登记在 `data/source_manifest.md`，一键录入 `scripts/ingest_sources.py`）。
- 起点指标（原作者 20 题评估）：可答题正确率约 **75%**；检索 hit@5 = **94%**。

---

## 1. 第一课：先建可信基线，再谈调优

**问题**：准确率低，但低在哪？凭感觉改参数必然白费。

**思考**：必须拆层——「检索（找没找到）」与「生成（用没用对）」是两个独立环节，混在一起看一个数字永远定位不了。

**怎么做**：写 `scripts/diagnose.py`——只跑检索、不调 LLM（零 API 成本），对每题自动判断「答案来源文件是否进入 top-5 / top-10」（hit@5 / hit@10）。

**踩坑与修正（命中率口径）**：
- 无答案题（来源=语料无）**不能进命中率分母**——它们本来就该 MISS，否则 15/20=75% 会被误判成"检索很差"。修正后分母=可自动判的题 → 15/16 = 94%。
- 多来源题（`来源: A + B`）出现「存在=False 却 HIT5」的矛盾——因为「存在」是整串精确匹配、「命中」是双向子串匹配，两套逻辑不一致。修正为按 `+` 拆开逐个判断。

**怎么判断该不该这么做**：诊断输出必须能指到报告里「哪一行证据」。说不出依据的结论 = 猜测。

---

## 2. 块级 vs 文件级命中：94% 是「假健康」

**问题**：hit@5=94% 很高，但 Q1/Q4/Q11 仍答错/拒答，矛盾。

**思考**：94% 是**文件级**命中（来源文件进了 top-5），不是**答案块**命中。打印 top-10 块内容核实。

**证据链（逐题）**：
- **Q1（RAG 全称）**：top-1 就是来源文件，但 grep 全语料，`Retrieval-Augmented` / `检索增强生成` **根本不存在** → **覆盖缺口**，模型拒答是正确行为。
- **Q11（切分器对比）**：相关块在 top-5，但语料只在代码示例里出现类名，**没有对比句** → **覆盖缺口**。
- **Q5（Embedding）**：答案在 `02_rag实战指南_langchain.md`，却被新语料的 langchain 函数文档抢走 top-10 → **真·检索漏召回**。
- **Q4（为什么切块）**：策略块在 top-2，但"最小单位"这句不在语料 → **部分覆盖缺口**。

**结论**：5 个错题里，真正属于系统错误的只有 Q5；其余是覆盖缺口/评估题缺陷。

**怎么判断**：对每个错题问三句话——① 答案在语料里吗（grep 关键词）？② 检索到答案块了吗（top-k 内容）？③ 模型用对了吗（评估回答）？三层的答案分别决定：改数据 / 改检索 / 改生成。

---

## 3. Round 1：生成层提示词（治「过度拒答 + 答不全」）

**问题**：料在、检到了，模型却答「资料中没有相关信息」（Q11），或答不全要点（Q12~14）。

**为什么这么改**：原 system prompt 的「如果资料中没有答案，请直接回答资料中没有相关信息」**太保守**——模型把「有相关内容但表述不同」也判成"没有"。

**怎么做**：
```
旧：只依据资料；没有答案就答"没有"
新：有相关内容务必尽力回答并完整列出要点；只有完全无关才拒答
```
同时把 `run_eval.py` 里**独立复制**的 ask_rag 与 `rag_qa.py` 统一（原代码是两份拷贝，改一处不会自动同步另一处）。

**怎么判断该不该这么做 / 做对了没有**：
- 前置证明：hit 高 + 拒答/不全 → 是生成层问题，不是检索问题；
- 改后对比：正确率 75% → **90%**，Q4/Q14/Q19 救回；**无答案题 Q15-18 仍 4/4 拒答（防线没破）** → 改对了；
- 副作用暴露：Q1 开始"答出全称"——但语料没有这个词，这是**记忆补全 = 新幻觉风险**。→ 提示词放宽是把双刃剑，**每次改动必须盯住无答案题是否仍然拒答**。

---

## 4. Round 2：BM25 混合检索（治精确术语漏召回）

**问题**：Q5「什么是 Embedding」答案在 02 文件，却被 langchain 函数文档抢走 top-10。

**为什么这么改**：Q5 是「精确术语」问题（数值向量/Embedding），向量语义检索对精确词天然弱；而 BM25 擅长精确匹配。但**先实验验证、再集成**，不盲改。

**怎么做（先实验 `scripts/exp_hybrid.py`）**：对 Q5/Q11/Q15/Q18 分别用「向量 / BM25 / RRF 融合」检索，看答案块进不进 top-10：
- Q5：「数值向量」块（全语料仅 1 块）BM25 能捞回（向量 0 → 融合 1）→ **值得上**；
- Q15：「GPT-5」语料 0 块 → 混合检索**不会凭空造答案** → 无答案题安全。

**怎么集成**：`rag_qa.py` 中「向量粗召回 2K + BM25 粗召回 2K → RRF 融合取 top-5」。**关键：最终送入 LLM 的上下文仍是 5 块，没有增加噪声**——只是候选池更准。

**怎么判断该不该这么做**：
- 回应用户质疑「扩 K 会引入污染」：**对**。所以不扩最终上下文，只扩大候选池再用 RRF 取两路共识，噪声不增反降；
- 改后：Q5 从拒答 → 答出；Q15-18 仍 4/4 拒答 → 成功；
- **最大教训**：评估工具（run_eval）和诊断工具（diagnose）必须用**同一套检索**。v3 评估里 Q5 已修复，但 diagnose 还在用纯向量，造成「改了还漏」的假象；统一后 hit@5 = **100%**。

---

## 5. 扩展测试集（不篡改真实用户问题）

**问题**：20 题全来自老 5 篇，衡量不了 481 篇新语料。

**为什么不能改原题**：真实业务里，用户不会因为答案不在语料而改问题。测试题只允许在「题目本身有硬伤」时修改；因为「当前语料没答案」而改题 = 作弊。

**怎么做**：新增 24 题，**全部基于真实语料内容出题**、每题标注确切来源文件、可自动判 hit；原 20 题原样保留 → `tests/questions_full.md`（44 题）。`config.py` 的 `QUESTIONS_PATH` 切到新文件。

**怎么判断**：新 24 题 hit@5 = **100%**（来源文件全部可检索到）→ 题目落地正确，不是拍脑袋。

---

## 6. 最终结果（扩展测试集，混合检索 + 新提示词）

| 指标 | 数值 |
|---|---|
| 检索 hit@5（可自动判 40 题） | **40/40 = 100%** |
| 可答题正确率 | **39/40 = 97.5%** |
| 无答案题拒答 | **4/4，零幻觉** |
| 剩余覆盖缺口 | Q1（RAG 全称）、Q11（切分器对比）→ 需补真实语料 |

**回顾**：可答题正确率 75% → 97.5%；检索 94% → 100%。两处覆盖缺口不是系统问题，是语料内容缺失。

---

# 系统化调优流程（沉淀）

## A. 分层定位决策树（每次先过它）
```
拿到错题 →
  答案要点在语料里吗？（grep 关键词）
  ├─ 不在 → 覆盖缺口：补真实语料；不是系统问题
  └─ 在 → 检索 top-k 里有没有答案块？
       ├─ 没有 → 检索层：切片 / 混合检索 / 查询改写 / embedding
       ├─ 有但在 top-5 外 → 排序层：重排 / K
       └─ 有且在 top-5 → 生成层：提示词 / 模型
```

## B. 指标口径（先定口径再看数）
- 检索层：hit@5 / hit@10（文件级，粗筛）；块级命中看 top-k 内容。
- 生成层：忠实度（有无编造）、完整性（要点覆盖）、相关性、拒答正确率。
- **分开统计**：语料覆盖缺口题不计入系统正确率；无答案题单独计拒答。

## C. 实验纪律
1. 一次只改一个变量；改前记基线（备份 answers.md）；
2. 改后对比「命中率 + 正确率 + 拒答」三组数，不能只看总分；
3. 评估工具与诊断工具用**同一套检索**，否则数据打架；
4. 提示词类改动必须盯住「无答案题是否仍然拒答」（防幻觉）；
5. 生成有随机性，正式测量跑 2~3 次取多数。

## D. 推荐调优顺序（按性价比）
1. 建基线 + 分层诊断（免费）；
2. 数据/切片（免费，最常被忽略）；
3. 提示词（免费，治拒答与完整性）；
4. 检索：混合检索 BM25+向量 RRF（低成本，治精确术语）；
5. 重排（召回充分后再上）；
6. embedding 升级（最高成本，最后做、必须 A/B）；
7. 换更强 LLM（生成层已确认瓶颈时）。

## E. 上线后的日志驱动
把线上「被拒答/答错」的真实问题回灌成测试集，按 A 的决策树分类，分别驱动补库 / 检索 / 生成改进——**用户问什么，就验什么**。