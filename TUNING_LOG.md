# kb_qa 调优日志

时间：2026-07 ~ 2026-09。目标：从「能跑」做到「可量化、可复现、评估与线上同源」。

原则：

1. 先量化基线，再单变量实验；改前留档（`tests/answers_*.md`、`tests/diagnose_v*.md`）。
2. 结论必须能指到报告里的一行证据。说不出依据 = 猜测。
3. **覆盖缺口 ≠ 系统错误**。评估措辞在语料里不存在，应补语料或改口径，禁止为刷分改题。
4. `diagnose` / `run_eval` / Web 必须调用同一套 `retrieve_hybrid`。
5. 动生成提示词后，必须回头看 Q15–18 是否仍拒答。

---

## 指标怎么读

| 口径 | 含义 | 何时用 |
|---|---|---|
| 文件 hit@5 | 金标来源文件名是否出现在 top-5 | 粗筛；容易虚高 |
| 短语 hit@5（库内可检） | 金标短语至少一条能在**全库**找到，才进分母；再看 top-5 **块文本**有没有 | **检索主指标** |
| 覆盖缺口 | 短语在全语料都找不到 | 语料 / 出题问题 |
| 块级漏召回 | 短语在库里，但 top-5 没有 | 切分 / 排序问题 |
| 字面短语 hit（含缺口） | 缺口题也进分母 | 只对照旧 62%，不作主指标 |
| 可答题正确率 | 人工/LLM 对照要点 | 生成层；与检索分开报 |
| 无答案题拒答 | Q15–18 | 幻觉闸门 |

无来源题不进检索分母。

---

## 总表

| 阶段 | 检索（可判 40 题） | 生成 | 闸门 |
|---|---|---|---|
| 原作者 20 题 + 纯向量 | 文件 hit@5 ≈ 94%（15/16） | 可答题约 75% | Q15–18 拒答 |
| 扩库 481 篇 + 提示词 | — | 75% → **90%** | 仍 4/4 |
| + 向量/BM25 RRF，评估与诊断同源 | 文件 hit@5 **40/40** | **39/40（97.5%）** | 仍 4/4 |
| 结构切分（字面口径） | 短语 23–24/40，块级漏召回 1→2 | 未重跑整表 LLM | — |
| + 重排 + 邻居（字面口径） | 短语 25/40，**块级漏召回 0** | 未重跑整表 LLM | — |
| **现口径**（混合+重排+邻居） | 文件 **40/40**，库内可检短语 **25/25**，缺口 15，块级漏召回 **0** | 97.5% 仍是「重排前」那次 LLM 评估 | 多轮后 Q15 仍拒答（手测） |

`run_eval.py --score-only` 曾打出 18/40（45%）：和旧字面短语 hit 一样，把「措辞 ≠ 原文」算成答错。不要当正确率。

---

## 0. 起点

- 语料：5 篇技术文 → **481 篇**（维基 120 / CMRC2018 100 / DRCD 80 / Chatchat 文档 25 / THUCNews 150）。来源：`data/source_manifest.md`，录入：`scripts/ingest_sources.py`。
- 原作者 20 题：可答题正确率约 75%，文件 hit@5 约 94%。
- 本仓库还做了：中文 embedding `bge-small-zh-v1.5`（相对 MiniLM，正确率大约 +17pt）、k=3→5、FastAPI、ReAct Agent。那些是扩库之前的课，不在下面重复展开。

---

## 1. 先建可信基线

**问题**：准确率低，但不知道低在检索还是生成。

**做法**：`scripts/diagnose.py` 只跑检索、不调 LLM。看金标来源文件是否进 top-5 / top-10。

**口径坑**：

- 无答案题本来就该 MISS，不能进命中率分母。否则 15/20=75% 会被说成「检索很差」。修正后 15/16=94%。
- 多来源题（`来源: A + B`）曾出现「存在=False 却 HIT5」：整串精确匹配 vs 子串命中两套逻辑。改为按 `+` 拆开再判。

诊断输出必须能指到报告里的具体 top-k 块。

---

## 2. 文件 hit 94% 是假健康

**问题**：文件 hit 很高，Q1 / Q4 / Q11 仍错或拒答。

**原因**：文件进了 top-5 ≠ 答案句子在块里。

| 题 | 事实 | 归类 |
|---|---|---|
| Q1 RAG 全称 | 来源文件 top-1，但全库没有 `Retrieval-Augmented Generation` / `检索增强生成` | 覆盖缺口，拒答合理 |
| Q11 切分器对比 | 类名在代码示例里，没有对比句 | 覆盖缺口 |
| Q5 Embedding | 答案在 `02_rag实战指南_langchain.md`，被 langchain API 文档挤出 top-10 | **真漏召回** |
| Q4 为什么切块 | 策略块在 top-2，「最小单位」这句不在语料 | 部分缺口 |

当时 5 个错题里，系统错误主要是 Q5。对错题问三句：要点在语料吗 → top-k 块有吗 → 模型用对了吗。分别对应补数据 / 改检索 / 改生成。

---

## 3. Round 1：提示词（过度拒答 + 答不全）

**现象**：块在 top-5，模型仍答「资料中没有相关信息」，或 Q12–14 要点不全。

**原因**：旧 prompt「没有答案就说没有」太死，把「相关但表述不同」也判成没有。

**改动**：有相关内容必须尽力答并列出要点；只有完全无关才拒答。同时删掉 `run_eval.py` 里复制的 `ask_rag`，评估改为调用 `rag_qa.ask_rag`。

**验收**：正确率 75% → **90%**（Q4 / Q14 / Q19 救回）；Q15–18 仍 4/4 拒答。副作用：Q1 开始「答出全称」，语料里没有这个词 → 记忆补全。以后每次放宽提示词都要盯拒答闸门。

留档：`tests/answers_v2_prompt.md`。

---

## 4. Round 2：向量 + BM25 RRF（精确术语）

**问题**：Q5「数值向量 / Embedding」被语义近、词面远的 API 文档抢走。

**实验**（`scripts/exp_hybrid.py`，先实验再合入）：

- Q5：该块全库仅 1 条；纯向量 0，融合后进 top-10 → 值得上。
- Q15 GPT-5：语料 0 块；混合检索不会凭空造答案。

**合入**：两路粗召回后 RRF，**送进 LLM 的仍是 top-5**，不靠扩 K 灌噪声。

**验收**：Q5 从拒答到能答；Q15–18 仍拒答。

**教训**：有一阵评估已走混合检索，诊断还在纯向量，出现「改了还漏」的假象。统一 `retrieve_hybrid` 后文件 hit@5 = **40/40**。

留档：`tests/answers_v3_20q_hybrid.md`。

---

## 5. 扩展到 44 题

20 题全来自老 5 篇，测不了 481 篇新语料。用户不会因为库里没有答案就改问题，所以 **原 20 题一字不改**；新 24 题全部从真实语料出、标注来源文件。文件：`tests/questions_full.md`。

新 24 题文件 hit@5 = 100% → 题是落地的，不是拍脑袋。

混合检索 + 新提示词后：**文件 hit@5 40/40，可答题 39/40（97.5%），拒答 4/4**。当时仍把 Q1 / Q11 记成「剩余覆盖缺口」。

---

## 6. 结构感知切分

**问题**：固定 500/100 滑窗会把标题和答案切开；文件 hit 100% 时仍可能块级漏召回。

**做法**：`scripts/chunking.py`。按篇检测，不按扩展名：Markdown `#` → 维基「词源。」类短行 → 空行/全角缩进段落 → 最后才 `RecursiveCharacter` 滑窗。每块加 `[来源] 文件 \| 标题路径`。`CHUNK_SIZE` / `CHUNK_OVERLAP` 只允许出现在 `config.py`。建库与 BM25 必须同一套切分，否则两路对不齐。

**对照**（当时还是字面短语口径，缺口在分母里）：

| 报告 | 设置 | 字面短语 hit@5 | 块级漏召回 |
|---|---|---|---|
| `diagnose_v0_old_chunk.md` | 旧滑窗 + 混合 | 24/40 | 1 |
| `diagnose_v1_struct_chunk.md` | 结构切分 + 混合 | 23/40 | 2 |

字面分没有变好看，甚至略差：切分改变了块边界，缺口题仍在分母里。所以这一步的验收不能看 24/40，要看后面重排能否把**库里有的那句**送进 top-5。

---

## 7. 重排 + 同文档邻居

**问题**：RRF 把相关文件召回来了，但最佳句不一定在精排 top-5；相邻小节常被切到下一块。

**做法**：RRF 池取 `RERANK_CANDIDATES=20`，`bge-reranker-base` 精排取 `K=5`，再按 `chunk_index` 左右扩 `NEIGHBOR_RADIUS=1`，总块数不超过 `CONTEXT_MAX_CHUNKS=8`。重排失败则降级为 RRF 顺序，问答不中断。

**对照**：`tests/diagnose_v2_rerank_neighbor.md`（字面口径）短语 25/40，**块级漏召回 0**。

没有再跑 44 题 LLM 评估，避免把「检索变好」和「生成波动」搅在一次实验里。生成数字仍以第 5 节的 97.5% 为准，并在 README 里写明时点。

---

## 8. 改正短语口径

**问题**：15 道覆盖缺口一直压着短语 hit，看起来像检索只有 62%（25/40）。

**改法**：短语 hit **只统计库内可检题**；缺口单独计数。字面口径保留作对照。

**现报**（`tests/diagnose_report.md`，混合 + 重排 + 邻居，k=10）：

- 文件 hit@5：**40/40**
- 短语 hit@5（库内可检）：**25/25**
- 覆盖缺口：**15**
- 块级漏召回：**0**
- 字面口径（含缺口）：25/40（62%）

Q1 仍是覆盖缺口：来源文件能召回，但评测要求的英文全称不在语料里。系统不必为这题「编出」全称。

---

## 9. 多轮（M1 → M2 → M3）

单轮检索已经顶到当前金标天花板（库内可检 100%，块级漏召回 0）。多轮要解决的是另一件事：追问里的「它」不能直接丢进向量检索。

顺序固定，不要对调：读记忆 → 改写 → `retrieve_hybrid` → 抽句压缩 → 生成（原问 + 短历史 + 压缩资料）→ 写回记忆。

### M1 短期 + 改写

- `session_memory.py`：按 `session_id` 留最近 3 轮；用户原句不截，助手答截到 400 字。空 id = 单轮，评估不传。
- `query_rewrite.py`：无历史跳过；完整新问题（≥8 字且无「它/刚才/呢」）跳过，防止换题仍灌旧实体；失败或超 80 字回退原问。
- 追问时把上一轮命中块 ±1 注入 RRF 池再重排（`used_anchors`）；换题不加。
- 演示页气泡 + `sessionStorage` 会话 id。手测：「什么是 RAG？」→「它的三个步骤是什么？」检索句变为 RAG 的步骤。

### M2 抽取压缩

- `context_compress.py`：2-gram 重叠抽句，不调 LLM。未超 `CONTEXT_MAX_CHARS=2800` 则原样通过。精排前 K 块至少留一句，避免 `[1]` 被压空。
- prompt 用压缩件；返回的 `hits` 仍是原文（来源预览、锚点）。
- 烟测「什么是 RAG？」8 块共 1768 字，未压缩，符合设计。

### M3 长期偏好

- `long_term_memory.py` → `data/memory/ltm.json`，按 `user_id`。只抽「只要某域」这类约束，不抽世界知识，**禁止 upsert 进 Chroma**。
- 请求未带 `domain` 时用偏好过滤检索；下拉框本轮可覆盖。纯偏好句（无问号、较短）只记约束，不检索、不生成，以免触发接口内容审核。
- 「新会话」只清短期；「清除偏好」才清 LTM。
- 验收：记住百科域 → 新 session 问「数学属于哪一类学科？」来源全是 `wiki_*.md`（`domain_source=ltm`）；同一偏好下问 GPT-5 参数，仍答「资料中没有相关信息」。

生成侧写与资料分栏。侧写不得当事实证据。

---

## 10. 生成侧其它约束

- 引用编号对应未压缩 `hits` 的顺序。
- 历史里助手的旧回答不能当依据。
- DeepSeek 偶发对部分维基人物返回 `Content Exists Risk`：接口层给出换题提示，不把 500 抛到页面。这是模型审核，不是检索失败。

---

## 沉淀

### 分层决策树

```
错题 →
  要点在语料里吗？
  ├─ 不在 → 覆盖缺口（补语料或改口径；不是检索 bug）
  └─ 在 → top-5 块有没有这句？
       ├─ 没有 → 切分 / 混合检索 / 重排 / 邻居
       ├─ 有但排序靠后 → 重排 / 邻居
       └─ 有且在上下文里 → 提示词 / 压缩误删 / 模型
```

### 实验纪律

1. 一次一个变量。
2. 改后同时看：文件 hit、库内可检短语 hit、块级漏召回、拒答。禁止只看总分。
3. 评估与诊断同一检索器。
4. 提示词改动必回归 Q15–18。
5. 词面自动分只能当辅助，不能当主正确率。

### 性价比顺序（后来也按这个走）

诊断（免费）→ 数据/切分 → 提示词 → 混合检索 → 重排/邻居 → 压缩与多轮记忆 → 最后才考虑更大 embedding / 更强 LLM。

---

## 刻意未做

- 默认用 Agent 当检索器（Agent 只包已测好的 `ask_rag`）。
- GraphRAG、换向量库、升级 embedding：现口径下块级漏召回已是 0，先不动最高成本件。
- 长期记忆写入向量库：会污染拒答闸门。
- 用 LLM 压缩每一块：8 块 8 次调用，比省下来的生成 token 更贵。
- 切分/重排之后未重跑 44 题 LLM：避免一次实验里混进生成波动。若要刷新 97.5% 这个数，应单独开一轮、改前备份 `answers.md`。
